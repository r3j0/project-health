export const API_PREFIX = 'api';

// Keep released versions stable; add a new version instead of changing this one.
export const API_V1 = '1';
export const API_V1_BASE_PATH = `/${API_PREFIX}/v${API_V1}`;
