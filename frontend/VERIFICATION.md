# 02번 프론트엔드 검증

## 2026-09-24: 실제 백엔드 PR 연동 수정

프론트 `feat/frontend/fitness-onboarding`의 실행 코드 `0f43ffd` 기준이다. 실제 백엔드 PR #6의 `70fd83e159b6f8d4b738442f4d735c73c3038215`를 별도 체크아웃하여 연결했다. 이전 임시 백엔드 구현과 DB 스키마는 사용하지 않았다. Node.js 26.7.0 / Next.js 16.3.5 프로덕션 빌드 / Chromium 모바일 환경이다.

### 수정과 구조 유지

1. `f5256e3`: 실제 `items[].evaluation`과 `axes`를 읽어 저장된 등급·6축·기준·다음 목표·출처를 표시한다. 최상위 종합 인증 미판정 때문에 종목 결과가 숨겨지던 문제를 수정했다. Decimal 경계 표기가 `57.0`/`57`처럼 달라도 동일 수치로 검증한다.
2. `a7a8c1e`: `/measurements/latest-polygon`에 연결하고 실제 빈 응답·revision·최신 한 회차를 검증한다. 메인·계정과 다른 탭 갱신은 기존 세션 무효화 구조를 유지한다.
3. `0f43ffd`: 새 간이측정을 `self_assessment`/`unknown`으로 저장한다. 수정 화면에서 기관 결과표 항목을 제거하고, YMCA의 실제 카탈로그 라벨·소수 입력 회귀를 갱신했다. 기존 미확정 저장 요청의 키·본문은 변경하지 않는다.

응답 검증은 `lib/fitness-contract.ts`, `lib/latest-fitness.ts`에 두고 표시용 상태·좌표는 `lib/fitness-evaluation.ts`에 남겼다. `FitnessRadar`, 기록 폼, 인증·초안·ETag·Idempotency-Key, `WorkoutRunner`를 재사용한다. 프론트에 판정표나 대표 등급 계산을 추가하지 않았다. 새로운 의존성·라우트·DB 변경은 없다. `backend/`에는 변경·미추적 파일이 없다.

### 검증 결과

- 각 기능 완료 시 `npm run check`(ESLint·TypeScript·단위 테스트·프로덕션 빌드) 통과. 최종 단위 테스트 67개 통과.
- 기능별 브라우저 검사: 상세 8개, 최신 대표 12개, 간이측정 15개 통과. 이 수치는 겹치는 시나리오를 포함하며 전체 검사와 합산하지 않는다.
- `npm run format:check`, `git diff --check` 통과.
- 전체 브라우저 회귀: 81개 통과, 실패·건너뛰기·자동 재시도 없음, 6.9분. 실제 API 기반 64개(일부 장애 응답 주입 포함), HTTP 계약 대역 17개.
- 실제 테스트 DB에 기존 운동을 배정한 별도 검증: 3개 통과. 간이측정 후 기존 배정 객체 유지, 실제 사진 API 503 후 직접 입력 저장, 320px 상세·메인·계정 화면의 6축·증가/감소 목표·가로 넘침 없음 및 브라우저 실행 오류 없음 확인.

전체 검사에는 실제 API 기반 테스트와 HTTP 계약 대역이 함께 있다. `fitness-live`의 4개 시나리오는 응답을 대체하지 않고 실제 저장 → 상세 → 메인·계정 → 수정·삭제 및 부분 간이측정을 확인한다. `fitness-report`/`latest-fitness`는 실제 응답 구조를 따르는 fixture로 오류·동시성·범위형/열린 경계를 검증한다. 이 fixture의 합성 경계 테스트를 서버의 실제 기준표 검증으로 해석하지 않는다.

다음 회귀를 확인했다.

- 가입·로그인·새로고침·로그아웃, 계정 변경·탈퇴, 실패·응답 유실·다른 계정 전환.
- 모든 등록 시작점, 직접 입력의 실제 0·음수·정밀도, 원문 등급 보존, 수정 충돌·이전 응답 차단, 초안 복구.
- 만 19~64세 간이측정의 타이머·중단·재측정·부분 저장·빈 측정 거부, YMCA 90bpm 저장 후 96.5bpm 수정, 신체정보/BMI, 재로그인 후 중복 없는 재시도.
- 실제 6종목의 등급 표시와 수정 재평가, 같은 축 최고 등급 선택, 미측정/정보 부족/기준 미확보/기준 미달/미평가 구별.
- 항상 6개 점을 원점까지 연결, 한 회차만 선택, 과거 회차로 빈 축 보충 금지, 수정·삭제·다른 탭 변경 후 갱신, 마지막 기록 삭제 후 온보딩 복귀.
- 사진 계약의 추출·확인·취소·429·초안 격리·55초 지연 응답과 실제 API 미설정 안내 후 직접 입력.

### 검증 한계와 백엔드 후속 논의

- 실제 OpenAI 키·모델이 없는 환경이므로 실물 결과지 인식 성공·품질은 미검증이다. 실제 미설정 503 처리와 계약 대역의 추출 성공을 구분한다.
- 현재 최신 선정은 백엔드의 `measuredOn DESC, id ASC`다. 같은 날짜의 마지막 저장순 정책은 별도 합의 사항이며 프론트가 바꾸지 않았다.
- `self_curl_up`·YMCA bpm의 호환 공식 기준 미확보는 서버 응답대로 평가 불가를 표시한다. 프론트에서 다른 종목 등급을 대입하지 않는다.
- 실제 iOS/Android 카메라·키보드·음향 및 Safari는 미검증이다. 타이머 E2E는 가상 시계를 사용하며 실제 운동 수행 품질을 판정하지 않는다.

이번 실행의 로그·스크린샷·JSON 결과는 작업 호스트의 `/private/tmp/project-health-frontend-integration-20260924/`에 저장했다. 저장소에는 테스트 소스와 현재 연동 계약을 커밋하며 계정 정보·환경 파일·실행 산출물은 포함하지 않는다.

---

아래는 이전 날짜의 검증 이력이다. 현재 계약과 구현 상태는 위 기록 및 [연동 문서](BACKEND-INTEGRATION.md)를 따른다.

## 2026-09-22: 백엔드 연동 준비

작업 경로 `project-health-frontend-02`, 브랜치 `codex/frontend-phase-two`. Node.js 26.7.0 / Next.js 16.3.5 프로덕션 빌드 / Chromium 모바일 환경에서 확인했다. 백엔드 PR #6의 확인 시점 head는 `61d46b75a483655fb6618de6478412e4a43b9502`다.

### 구현 및 계약 경계

- 일반 운동과 간이측정 모드를 분리했다. 간이측정 저장은 기존 사용자 커리큘럼을 변경하지 않는다. 인증 만료 후 mode와 진행 중 입력을 복원한다.
- 사진 추출 API에 multipart 업로드·취소·오류/429·검토·수정·기존 API 저장을 연결했다. 사진 초안은 직접 입력 초안과 격리하며, 사용자가 확인 후 사진만 다시 선택할 수 있다. 저장 중·결과 불확실 상태에는 초안을 폐기하지 못한다.
- 서버 판정에 따라 6축을 항상 유지하고 원점까지 연결한다. 0/1/2개 평가, 기준 미달, 미입력/미지원 사유, 원본 결과표 등급 분리를 검증했다. 잘못된 id/revision/원본 값·단위/등급 응답은 차트로 표시하지 않는다.
- 회차별 상세 리포트와 메인·내 프로필의 최신 한 회차 대표 프로필을 연결했다. 저장·수정·삭제·다른 탭 변경·화면 복귀 이후 이전 차트가 남지 않는다.
- 평가와 대표 API의 URL·필드명은 **확정 전 제안**이다. 사진 추출만 현재 PR 명세를 사용한다. 상세 계약과 실제 연결 후 확인 항목은 [백엔드 연동 문서](BACKEND-INTEGRATION.md)에 있다.
- 이 브랜치의 `backend/`는 `origin/main`과 차이가 없다. 기존 백엔드 코드를 복구하거나 추가하지 않았다.

### 최종 전체 재검증 결과

프론트 실행 코드 `2ffec35` 기준으로 최종 빌드를 새로 실행한 뒤 검사했다. 아래 결과는 이전의 부분 재실행 결과가 아니라 전체 회귀를 한 번에 실행한 결과다.

- ESLint, TypeScript, 프로덕션 빌드, Prettier, `git diff --check` 통과.
- 최종 단위 테스트 59개 통과.
- 브라우저 전체 74개 통과, 실패·건너뛰기·자동 재시도 없음, 6.8분 소요.
- 기존 실제 API 기반 회귀 60개: 인증·계정 변경/탈퇴, 기록 CRUD·ETag 충돌·응답 유실·초안 복구, 등록 상태 갱신, 간이측정 타이머·부분 저장·재측정·재로그인. 장애 시나리오 일부는 의도적으로 응답을 차단/변경한다.
- 신규 HTTP 계약 검사 14개: 간이측정 모드 복귀·기존 커리큘럼 유지, 사진 추출/확인/초안 격리/취소/429, 6축과 상세 기준, 최신 한 회차 프로필·변경 후 재조회·늦은 응답 차단.
- 새 사진 지연 회귀는 실제 시간으로 55초 뒤 응답한다. 기존 50초 제한에서 연결 오류가 발생함을 먼저 재현했고, 90초로 수정한 최종 빌드에서는 55.5초에 확인 화면 진입까지 통과했다. 일반 요청의 20초 제한과 사용자의 분석 취소는 변경하지 않았다.
- 메인·내 프로필·상세 차트의 모바일 스크린샷을 확인했다. 320px 가로 넘침과 브라우저 실행 오류가 없었다. 원점으로 연결된 하나의 유효 축도 차트에 남는다.

### 회귀에서 확인하고 수정한 사항

1. 인증 복귀가 pathname만 사용해 간이측정 mode가 사라질 수 있었다. 허용한 로컬 경로만 복원하면서 assessment 쿼리를 유지한다.
2. 사진 미리보기가 접혀 있어 기존 확인 흐름과 달라졌다. 사진 선택 직후 원본이 보이게 복원했다.
3. 사진 확인 초안 복귀를 직접 입력 경로와 혼용하던 테스트를, 사진 전용 초안 자동 복원 흐름에 맞췄다. 재선택 시 직접 입력 초안은 유지한다.
4. 대규모 실제 인증 회귀 중 IP 요청 제한에 걸렸다. 제한을 낮추거나 해제하지 않고 15개 시나리오 단위로 시간창을 나눴으며 지연 응답·초안 보존 시나리오가 통과했다.
5. 대표 API가 상세 판정 항목 전체를 요구하지 않도록 요약 검증과 상세 검증을 분리했다. 대표 응답은 한 회차의 6축과 식별 정보만으로 연결할 수 있다.
6. 최신 백엔드 PR `61d46b7`은 사진 추출 요청 제한을 45초에서 75초로 늘렸다. 프론트의 50초 제한이 정상 응답을 먼저 취소하는 결함을 발견해 90초로 맞췄다. 코드·재현 회귀·연동 문서를 `2ffec35`에 커밋했다.

### 검증 한계

기존 실제 API 회귀는 이미 실행 중이던 전용 로컬 PostgreSQL과 이전 PR #6 사용자 API + 기존 간이측정 실험 지원 런타임을 사용했다. 현재 PR #6 head에 간이측정 저장 지원이 추가됐다는 뜻이 아니다. 새 사진 추출·평가·대표 프로필 브라우저 검증은 명시적인 HTTP 계약 대역을 사용하며 운영 경로에는 대역이 없다.

이번 프론트 재검증에서는 실제 OpenAI 호출을 실행하지 않았다. 백엔드 PR 자체의 실제 호출 보고와 별개로, 최신 OCR 서버를 통한 프론트 전체 연동·판독 품질은 미검증이다. 백엔드 등급 경계/대표 종목 선택/동일 트랜잭션 저장, 새 대표 API의 최신 회차 정렬도 아직 검증하지 않았다. 백엔드 구현과 실제 계약이 공개된 뒤 통합 검증해야 한다. 실제 iOS/Android 카메라·키보드·소리 및 Safari도 별도 범위다.

---

## 아래는 2026-09-21 당시 검증 이력

이하의 미구현 범위·백엔드 확장 관련 설명은 당시 기록이다. 현재 범위와 백엔드 원복 상태는 위의 2026-09-22 기록 및 연동 문서를 따른다.

검증일: 2026-09-21. 브랜치: `codex/frontend-phase-two`.
백엔드: PR #6의 `898b2a480d37438ed55ca1860fe519cc732fd695`, 전용 로컬 PostgreSQL.
프론트엔드: Next.js 16.3.5 프로덕션 빌드, Node.js 26.7.0, Chromium 모바일 환경.

## 목표 달성 판정

현재 백엔드 계약으로 연결할 수 있는 기능 및 2026-09-21 추가 요청의 성인 간이측정 실행·저장을 구현했다. 회의의 02번 전체 목표를 완료했다고 판단할 수는 없다. 사진 자동 추출, preference 저장, 커리큘럼 자동 배정·실행에 필요한 API가 없고, 다각형 프로필·리포트·내비게이션 확장은 요청에 따라 제외했다.

| 항목                                 | 판정                  | 확인한 동작과 남은 범위                                                                                             |
| ------------------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 계정 설정                            | 제공 API 범위 완료    | 이메일·비밀번호 변경, 영구 탈퇴, 재화 잔액 조회. preference 필드는 PR #6 계약에 없다.                               |
| 공통 사용자 상태                     | 완료                  | `/auth/me` 공유, 측정 저장·삭제 후 갱신, 다른 탭 변경 반영, 실패 재시도, 구형 응답 거절.                            |
| 온보딩 직접 입력                     | 완료                  | 초안 복원, 기존 측정 폼으로 실제 저장, 메인 이동, 마지막 기록 삭제 후 미등록 상태 복귀.                             |
| 사진 입력                            | 부분 완료             | 촬영/선택·유효성 검사·미리보기·사진을 보며 직접 입력. 자동 추출은 미제공 API로 인해 미연결.                         |
| 메인                                 | 제공 데이터 범위 완료 | 미등록·등록 상태와 커리큘럼 미배정·배정·완료 구분. 미등록 사용자에게 간이측정 커리큘럼을 서버 배정하는 동작은 없다. |
| 운동 페이지                          | 성인 간이측정 완료    | 만 19~64세 실제 타이머·입력·부분 저장·수정·복원. 추천 운동 배정 완료나 보상 지급은 후속 범위.                       |
| 다각형 프로필·리포트·내비게이션 확장 | 이번 범위 제외        | 사용자가 이후 진행하기로 한 6번.                                                                                    |

`isOnboarded`는 별도 온보딩 단계가 아니라 실제 측정 기록의 존재 여부다. 성인 간이측정을 실제 저장하면 `true`로 갱신된다. 저장하지 않은 측정 진행은 등록으로 간주하지 않는다. `currentCurriculum`은 정의의 ID·이름과 배정 상태만 제공하므로 실제 운동 시작을 활성화하지 않는다. 배정/완료 화면 검증에는 명시적 HTTP 응답 fixture를 사용했다.

## 재현하여 수정한 문제

1. **운동 진행 되돌아감:** 기존 저장값이 있는 상태에서 저장소 쓰기가 실패하면, 최신 메모리 값보다 과거 저장값을 우선 읽었다. 최신 메모리를 기준으로 복원하도록 수정했다.
2. **폐기한 초안 재등장:** 저장소 삭제가 실패하면 로그아웃·초안 폐기 후 운동/측정 입력이 다시 복원될 수 있었다. 삭제 표시를 메모리에 유지하고, 저장소 삭제만 실패한 경우에는 빈 값으로도 덮어쓴다. 저장소 접근이 전부 막힌 환경의 보장은 같은 문서 안으로 한정된다.
3. **계정 변경 결과 불확실 시 복구 동선 부재:** 서버 변경은 성공했으나 브라우저 응답만 유실되면 재로그인 안내만 보였다. 중복 제출을 잠그고 `다시 로그인하기`를 제공한다. 선택 시 모든 탭의 로컬 인증·초안을 정리한다. 현재 비밀번호 오류는 자동 재전송하지 않으며 입력 수정 후 재시도할 수 있다.

회귀 테스트는 수정 전 실패를 확인한 뒤 추가했다. 탈퇴 응답 유실은 서버가 처리한 경우와 처리하지 않은 경우를 각각 검증하며, 재로그인이 실제 서버 결과를 따르고 탈퇴 요청이 반복되지 않는지 확인한다.

## 이전 02번 검증 결과

- 단위 테스트: 38개 통과.
- 브라우저 E2E: 두 번째 검증의 최종 수정본 전체 50개 통과 (3.6분).
- ESLint, TypeScript, Prettier, 프로덕션 빌드 통과.
- 실제 API로 가입·로그인·갱신·로그아웃, 계정 변경·탈퇴, 측정 CRUD와 온보딩 반영을 확인했다.
- 응답 지연·유실·401/403/409/412/429/503, 중복 제출, 다중 탭, 계정 전환, 초안 만료·저장소 실패, 화면 이탈·히스토리 이동·새로고침을 검증했다.
- 320px 너비에서 주요 6개 화면의 가로 넘침을 확인했고, 운동 하단 버튼을 스크롤 후 클릭해 고정 메뉴에 가려 막히지 않는지 추가 검증했다.
- 사진은 화면 이탈 시 폐기되는 것이 정상이다. 사진으로 입력하던 측정 초안은 `사진 없이 직접 입력하기`에서 복원된다.
- 인증 요청 제한은 해제하지 않았다. 02번 E2E 파일은 이전 파일의 요청량 때문에 실패하지 않도록 다음 분의 제한 구간부터 시작한다. 최초 검증에서 발생한 429와 접근성 알림 선택자 중복은 테스트 실행 문제로 수정했다.

## 두 번째 검증에서 추가로 재현한 문제

기존 47개 E2E는 다시 모두 통과했지만, 추가한 세 시나리오는 수정 전 프로덕션 빌드에서 모두 실패했다.

- **저장 응답 유실:** 서버에 첫 측정 기록이 저장되어도 메인은 계속 미등록으로 안내했다.
- **마지막 기록 삭제 응답 유실:** 서버에서 기록이 삭제되어도 메인은 등록된 상태로 남았다.
- **화면 복귀 갱신 누락:** `visibilitychange`만 발생하고 `focus`가 없는 복귀에서는 다른 기기에서 만든 기록을 반영하지 않았다. 실제 기기를 대신해 브라우저 이벤트로 재현했다.

측정 변경 요청의 통신 오류·서버 오류에서도 같은 로그인 세션에 한해 `/auth/me`를 다시 조회한다. 등록 여부를 낙관적으로 바꾸거나 변경 요청을 자동 재전송하지 않는다. 기존 폼의 미확정 요청·중복 방지 키는 유지한다. 사용자 상태 공급자는 `focus`와 `visibilitychange` 모두에서 현재 화면이 보일 때 갱신한다.

추가 회귀 테스트는 `tests/e2e/phase-two-profile-recovery.spec.ts`에 있으며, 실제 서버 저장·삭제 후 브라우저 응답만 유실시키고 변경 요청이 한 번만 전송되는지 확인한다.

## 검증 한계

실제 iOS/Android 카메라·키보드와 Safari는 이번 자동 검증 범위에 포함하지 않았다. 사진 자동 추출, 실제 추천 알고리즘, 커리큘럼 실행·보상 지급은 동작한다고 간주하지 않는다. 후속 API 계약이 정해지면 그 계약에 맞춘 연동 검증이 필요하다.

## 성인 간이측정 추가 검증 (2026-09-21)

PR #6 백엔드에 이 브랜치의 `self_assessment` 생성/수정 검증과 추가 카탈로그 마이그레이션을 적용한 로컬 PostgreSQL 환경에서 검증했다.

- 만 19~64세, 근지구력 검사 하나 선택, 항목 건너뛰기, 공란/실제 0/음수 구분.
- 3초 준비 후 교차 윗몸일으키기 60초, YMCA 180→60→10초 자동 연결 및 맥박×6 환산. 콜백 지연에도 경과 시간 기준을 유지한다.
- 중단·숨김·새로고침은 현재 측정 시도를 무효화한다. 이전 입력값은 유지한다. 체력검사를 일시정지해 계속한 결과를 저장하지 않는다.
- 부분 기록 생성·자가측정 출처·기록 수정·온보딩 갱신, 응답 유실/재로그인 후 원래 생성 키·본문 유지와 중복 방지.
- 화면 이탈·재진입, 24시간 초안 만료, 저장소 실패, 계정 전환/로그아웃의 초안 격리.
- 신장·체중 수정/삭제에 맞춰 BMI 재계산/제외. 미입력 자료로 값이나 점수를 만들지 않는다.
- 모바일 측정 중 하단 메뉴가 박자 안내를 가리는 문제를 발견해 측정 중 메뉴를 숨기고 타이머/중단 버튼을 같은 뷰포트에 배치했다.

실제 iOS/Android 기기의 소리 출력과 화면 잠금 동작은 검증하지 않았다. 브라우저 소리 안내는 선택 기능이며 시각적 박자 안내가 함께 제공된다. 일반 운동 추천, 서버 배정 완료 처리 및 보상 지급은 구현됐다고 간주하지 않는다.

최종 검사 결과:

- 프론트엔드 단위 테스트 43개 통과.
- 브라우저 E2E 전체 59개 통과(4.5분), 이후 완료 화면의 새 측정 시작 추가 사례 1개 통과. 총 60개 시나리오.
- 백엔드 단위 테스트 46개, 격리 DB/API 통합 테스트 111개 통과. 추가 마이그레이션 적용 후 재실행 시 미적용 항목이 없는 것도 확인.
- 프론트엔드 ESLint·TypeScript·Prettier·프로덕션 빌드 및 백엔드 린트·타입·빌드 통과.
- 전체 검사 최초 실행의 1건은 로컬 인증 요청 제한(429)으로 정지했다. 간이측정 테스트 파일도 다른 인증 테스트와 같은 방식으로 새 제한 구간에서 시작하도록 분리한 뒤 모두 통과했다. 제한은 해제하지 않았다.
- 수정한 로컬 서버를 실행한 상태이며, 실제 사용자 계정에는 테스트 측정값을 저장하지 않았다. E2E의 전용 테스트 계정/로컬 DB를 사용했다.
